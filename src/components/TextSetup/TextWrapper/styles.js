const styles = {
  wrapper: {
    position: "absolute",
    whiteSpace: "nowrap",
    zIndex: 1300,
    "& > span": {
      "& > div": {
        cursor: "pointer !important"
      }
    }
  }
};

export default styles;
